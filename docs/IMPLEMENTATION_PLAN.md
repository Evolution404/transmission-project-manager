# 实施计划与验收清单

状态：P0、P1、P1.1、P1.2、P2、P3、P4、P5、P6 已完成并通过本地/合成数据验收；P7 进入下一阶段。P1/P1.1 的 Cloudflare Access 认证仅保留在历史完成记录中，当前运行时认证链已经完全替换为系统自维护账号与服务端会话。

## 阶段总览

| 阶段 | 范围 | 依赖 | 状态 |
|---|---|---|---|
| P0 | 仓库、可运行骨架、CI、设计和交接 | 无 | 已完成 |
| P1 | 身份权限、基础配置、迁移与应用框架 | P0 | 已完成（本地验收通过） |
| P1.1 | 成员管理、首次登录、首管理员保护、可移交运维 | P1 | 已完成（后续认证方式由 P1.2 替换） |
| P1.2 | 系统自维护账号密码认证、会话与密码管理 | P1.1 | 已完成（本地验收通过） |
| P2 | Excel导入、需求池与物资字典 | P1.2 | 已完成（本地验收通过） |
| P3 | 储备归并、数量分配、估算及分类 | P2 | 已完成（本地验收通过） |
| P4 | 框架、协议、预算与费用流水 | P1、P3项目身份 | 已完成（本地验收通过） |
| P5 | 出库、实施、结算、四状态反馈 | P3、P4 | 已完成（本地验收通过） |
| P6 | 月报、预警、年度节点、通知与备份 | P2–P5 | 已完成（本地/合成数据验收通过） |
| P7 | 标准模板真实数据验收、性能与正式部署 | P1.2–P6 | 本地准备进行中；真实环境验收仍阻塞 |

后续 AI 进入 P7。P0–P6 的业务取舍和不变量已经固化，不应在 P7 为接入真实数据而静默改口径。需求数据采用“系统标准模板下载 → 用户填报 → 回导”的主流程，不要求系统适配任意历史 Excel；P7 负责把真实填报值、真实云资源、邮件、网络、恢复和运维移交逐项验收。

## 跨阶段测试门禁（2026-09-12 已建立）

P1.2 起全部采用“先测试、后生产代码”。详细规则见 `docs/TESTING.md`。当前门禁已经覆盖：当前开发 schema 的显式迁移锁、P1.2→P2 数据保留升级、空库重复迁移、账号初始化/登录/改密/重置/锁定/停用、production 会话认证、成员并发降权、失败写入原子性、幂等审计，以及 P2 的浏览器表格解析、字段映射、分片上传/校验/发布、需求追溯、物资字典、版本冲突和并发原子性；Vue 登录、成员管理、需求页与路由行为合同也纳入类型检查和完整门禁。

每个新阶段开始前先把该阶段不变量写成自动测试；缺陷修复必须先补能复现旧问题的回归测试。任何阶段只有完整 `npm run check` 全绿才能标记完成。禁止通过 `.skip`、`.only` 或 `test.todo` 绕过门禁。当前尚未正式上线，因此允许在明确业务决定下重整开发 schema，并同步迁移锁和空库测试；一旦进入正式/共享数据环境，迁移立即冻结为只追加模式。

## P0：初始化（本轮交付）

- [x] npm workspaces：Vue前端、Workers接口、共享类型。
- [x] 本地D1/R2占位配置，默认不公开workers.dev或预览URL。
- [x] 前端占位页、`/api/health`、未知API的JSON 404。
- [x] 类型检查、前端构建、Workers dry-run、真实本地workerd路由冒烟检查入口。
- [x] CI配置及设计、数据模型、运行部署、AI交接文档。

GitHub推送及CI结果以仓库提交和Actions实际记录为准。P0没有实现业务表、认证、邮件或生产部署。

## P1：身份与基础配置

责任范围：`apps/api`身份/权限中间件与初始迁移、`packages/shared`身份/配置类型、`apps/web`路由布局与成员入口。引入Vue Router和Naive UI；图表留到P6再引入ECharts。

交付：Access JWT验证、成员角色与项目范围、管理员配置、字典基础表、空状态页面、配置版本管理。开发测试身份只在明确本地模式中可用，生产缺少Access配置必须拒绝业务请求。新增云资源或部署根据当次用户授权执行，不能因缺少云账号便把本地功能全部停工。

验收：缺失/伪造/过期JWT拒绝；角色越权与跨项目访问拒绝；停用成员失效；配置修改有历史；真实业务接口不信任客户端邮箱；现有健康与API路由测试继续通过。

P1完成记录（2026-09-12，主要实现提交 `4880918`）：

- 建立 `members`、`member_scopes`、`settings_versions`、`dictionary_items`、`audit_events`、`idempotency_records` 初始迁移；合成本地成员只存在于 `seeds/local.sql`，不会随正式迁移进入生产。
- 生产模式使用 Cloudflare Access JWT 的签名、issuer、audience 和 email claim 验证；Access 配置缺失时业务接口 fail-closed。开发/测试模式仅允许显式本地身份。
- 实现 `/api/me`、`/api/members`、`/api/settings`、`/api/dictionaries` 与范围检查接口；配置写入带版本检查、幂等键、审计和历史版本。
- Vue Router + Naive UI 管理台框架已建立；六个后续业务入口保持真实空状态，规则与成员页读取真实 P1 API；重型管理页按路由懒加载。
- 从空本地 D1 状态执行 `npm run check`：TypeScript、前端构建、Worker dry-run 及 9 项 workerd+D1 集成测试全部通过；另实测 `APP_ENV=production` 且缺 Access 配置时 `/api/me` 返回 503 `AUTH_CONFIG_MISSING`。
- 尚未验证真实 Cloudflare Access 租户/JWT、线上 D1/R2 或大陆网络；这些属于生产配置/P7 验收，不据此宣称已上线。

## P1.1：成员管理与可移交运维

责任范围：补齐 P1 当前只有“查询”的成员管理闭环，并把“系统可完全移交、无需共享资产所有者 Cloudflare 账号”固化为可验收能力。应用侧由 `apps/api`、`apps/web`、`packages/shared` 负责成员生命周期和范围授权；Cloudflare/GitHub 的正式服务身份与交接演练在 P7 完成。

交付：

- 新增成员接口与页面：姓名、规范化邮箱、角色、启用状态、授权范围；支持编辑、停用、恢复，不提供会破坏审计引用的普通硬删除。
- 增加范围管理接口，至少支持 `all`、`framework`、`project`；服务端独立校验，前端隐藏按钮不能代替权限控制。
- 追加迁移记录成员 `invited_at`、`first_login_at`、`last_login_at` 等必要生命周期字段；已有 P1 迁移不回改。
- 成员状态在 UI 中区分“待首次登录 / 正常 / 停用”；第一次成功 Access 身份匹配后记录首次登录，后续更新最近登录。
- 不做开放注册。Access 验证通过但成员表不存在的邮箱仍返回 403，不自动建号、不自动授予角色。
- 明确首个真实管理员 bootstrap：必须显式指定邮箱，仅当成员表为空时可执行；创建后失效。禁止“第一个登录者自动成为管理员”。
- 后端保护“至少一个启用管理员”不变量：不能停用或降权最后一个启用管理员；所有角色、范围和状态变更写审计并带版本检查/幂等键。
- Access 策略优先按稳定企业邮箱域或 IdP 组覆盖常规用户，使应用管理员新增同组织成员时无需进入 Cloudflare；超出现有 Access 范围的特殊成员由受托技术维护人员使用自己的 Cloudflare 成员身份处理，不使用资产所有者账号。
- 编写《运维移交检查表》初稿，明确资产所有者、应用管理员、技术运维负责人、CI 服务身份四类责任边界；任何生产 Secret、Cloudflare Token 和 GitHub Secret 均不进入仓库。

验收：

- 管理员可在应用内新增成员并配置角色/项目范围；同一邮箱重复创建拒绝或幂等返回，不产生两个账号。
- 新成员在 Access 已覆盖的情况下，不需要 Cloudflare 后台逐人操作即可完成首次登录；首次登录后状态从“待首次登录”变为“正常”。
- 未加入成员表的 Access 身份、停用成员、角色越权、跨框架/项目访问均被服务端拒绝。
- 管理员不能停用或降权最后一个启用管理员；并发修改使用版本检查，冲突返回 409。
- 只读/实施/财务等角色不能调用成员管理写接口；客户端提交邮箱/角色不能绕过服务端当前身份判断。
- 角色、范围、启停和 bootstrap 均有审计记录；停用后下一次请求立即失效，历史审计仍可追溯。
- 本地/CI 使用合成 Access 身份完成完整成员生命周期测试，不把测试账号写入正式迁移。
- P1.1 完成后重新运行 `npm run check`，并将“下一步”切换为 P2。

P1.1 完成记录（2026-09-12，主要实现提交 `1b8139c`）：

- 新增 `0002_p1_1_member_lifecycle.sql`，只追加 `invited_at`、`first_login_at`、`last_login_at` 等生命周期字段，不回改 P1 迁移。
- 实现一次性 `POST /api/bootstrap/admin`：必须与显式 `BOOTSTRAP_ADMIN_EMAIL` 身份匹配且成员表为空；创建后自动关闭，不采用“首个登录者自动当管理员”。
- 实现 `POST /api/members` 与增强后的 `PATCH /api/members/:id`，支持成员新增、角色、启停和 `all/framework/project` 范围统一维护；重复邮箱拒绝，成员更新先识别幂等重放再校验版本。
- 登录成功后首次/最近登录时间自动更新，登录遥测不增加成员业务版本；UI 区分“待首次登录 / 正常 / 停用”。
- 后端强制“至少一个启用管理员”：最后一个启用管理员不能被停用或降权；普通角色不能调用成员写接口。
- “规则与成员”页面已提供新增、编辑、停用/恢复、角色与范围配置；管理员固定全范围，普通成员可配置全部或指定框架/项目。
- 新增 `docs/OPERATIONS_HANDOVER.md`，明确资产所有者、应用管理员、技术运维负责人、CI/CD 服务身份的责任与 P7 正式移交验收项。
- `npm run check` 全绿：TypeScript、前端构建、Worker dry-run 及 12 项 workerd+D1 集成测试全部 PASS；测试使用空临时 D1 从 bootstrap 开始，不依赖正式迁移中的合成账号。
- 尚未验证真实 Cloudflare Access 租户、正式成员邮箱和线上 account-owned token/CI/CD 移交；这些保留到 P7，不能据本地 P1.1 验收宣称正式上线。

## P1.2：系统自维护账号密码认证

最终业务决定（2026-09-12）：业务用户登录不依赖 Cloudflare Access、邮箱验证码或企业 IdP。系统自己维护账号、认证凭据、会话、角色和业务范围；邮箱不进入账号模型。Cloudflare 只承担 Workers、D1、R2、域名/WAF 等基础设施能力。

实现基线：

- 登录标识为大小写不敏感唯一 `username`。管理员创建账号时配置账号、姓名、初始密码、角色、启停和 `all/framework/project` 授权范围。
- 明文密码只存在于浏览器输入内存中。浏览器 Web Worker 使用 Argon2id 派生 32 字节凭据，当前参数为 `m=19456 KiB`、`t=2`、`p=1`、Argon2 v1.3；重计算不占 Worker CPU。
- 服务端不执行 PBKDF2/Argon2。服务端只计算 `HMAC-SHA256(AUTH_CREDENTIAL_PEPPER, derivedCredential)` 并保存 verifier；数据库同时保存每账号随机 salt 和 KDF 参数版本，既不保存明文密码，也不保存浏览器派生凭据。
- `/api/auth/kdf` 对已知和未知账号都返回同形态公开参数；未知账号使用确定性假 salt，登录失败统一返回“账号或密码错误”，避免直接枚举账号。
- 密码最少 15 个字符、最多 128 个字符；允许空格和 Unicode，不设置无意义的字符组合规则。管理员设置的初始密码和重置密码均要求用户首次/下次登录强制修改。
- 连续 5 次失败登录会临时锁定账号；成功登录清零失败计数。安全事件写审计。
- 登录成功签发 256 bit 随机会话令牌，D1 只保存 SHA-256 token hash；Cookie 为 HttpOnly、SameSite=Strict，生产为 Secure，默认绝对有效期 7 天。
- 退出、改密、管理员重置密码和账号停用会撤销相关会话；旧 Cookie 下一次请求立即失效。
- 首次登录且 `must_change_password=true` 时只允许 `/api/me`、修改密码和退出，不能进入业务接口。
- 首管理员只允许一次性 bootstrap：系统成员表为空且 `X-Bootstrap-Token` 与运行时 `BOOTSTRAP_TOKEN` 匹配时才能创建。普通请求永远不能“第一个登录自动当管理员”。
- 后端继续强制角色和范围权限；最后一个启用管理员不能被停用或降权。该约束已经进入数据库条件更新，覆盖并发降权竞态。
- 生产运行时不读取 `Cf-Access-Jwt-Assertion`、`X-Dev-User-*` 等身份头；这些头在 production 测试中被明确验证为无效。

开发期 schema 决策：系统尚未投入正式使用、没有需要保留的真实生产数据，因此按用户明确要求直接重整 `0001` 为当前正确结构，删除旧 `0002` 和本地账号 seed，不建立兼容层。当前迁移锁只约束这份开发基线；正式部署/共享数据出现后恢复只追加迁移规则。

交付已完成：

- `members` 直接包含 `username`、verifier/salt/KDF 参数、强制改密、登录失败计数、锁定状态、`session_version` 和生命周期，不再存在邮箱认证字段或额外账号兼容表。
- 新增 `auth_sessions`；实现 `/api/auth/status`、`/api/auth/kdf`、`/api/auth/bootstrap`、`/api/auth/login`、`/api/auth/logout`、`/api/auth/change-password`、管理员重置密码，以及成员创建/编辑接口。
- 登录页、首次强制改密页、退出登录和管理员账号/密码重置界面已完成。Argon2id 在独立 Web Worker + WASM 中运行，不阻塞页面主线程。
- 修复成员 PATCH 审计 SQL 占位符错误，并将“至少一个启用管理员”升级为数据库条件写入，确保并发请求不能把管理员数量降为 0。
- `npm run check` 全绿：31/31 Node/workerd+D1 测试、11/11 Vue 行为测试，共 42 项 PASS；Vite 成功产出独立 Argon2 worker 与约 28.9 kB WASM，Worker dry-run 通过。

P1.2 完成记录（2026-09-12，主要实现提交 `ad05dd0`）：完整 `npm run check` 全绿，31/31 Node/workerd+D1 + 11/11 Vue，共 42 项 PASS；Argon2 worker/WASM 生产构建和 Worker dry-run 均通过。

未完成/未验证：真实 Cloudflare 线上 CPU/网络/配额仍留 P7；客户端 Argon2id 的低性能手机体验需要在真实设备验收。P1.2 本身已经完成，下一步恢复 P2。

## P2：需求导入与物资

完成状态（2026-09-12）：P2 已在 P1.2 系统账号/会话认证基线上完成；恢复用 stash 后续已按用户要求删除，当前实现完全不依赖 stash。旧认证测试夹具、路由占位和 P2 并发写入边界均已修正，健康接口阶段已更新为 `p2`。

责任范围：导入接口、需求与物资迁移、浏览器解析Web Worker、字段映射与需求列表/详情、共享输入输出类型。

交付：多个Excel文件/工作表导入、映射模板、类型和业务校验、分片暂存及发布、同源幂等、疑似重复人工处理、变化预览、来源追溯。支持 `.xlsx` 和 `.csv`；旧 `.xls` 先提示另存为 `.xlsx`，不伪称支持。浏览器解析库在该阶段从官方源选定并锁定版本，避免将完整文件交给免费Worker解析。

验收：合成多表不同列名、重复文件、合法相同需求、缺字段、未知型号、未核实电压、负数/过精度数量；中断后继续；更改同一请求键负载产生冲突；失败批次不进入正式报表；列表接口固定分页上限，5万行规模不得通过单请求全量返回前端或交给 Cloudflare Worker 解析完整工作簿。

P2 完成记录（2026-09-12，主要实现提交 `24abfa0`）：

- 浏览器独立 Web Worker 解析 `.xlsx`/UTF-8 `.csv`，保留多工作表、源行号与原始字段；`.xls` 明确拒绝并提示另存为 `.xlsx`。导入文件 SHA-256 在浏览器计算，Cloudflare Worker 只接收最多 20 行的规范化源行分片并重新校验。
- 实现字段自动映射/人工映射与可复用模板、导入批次恢复、错误/警告分类、最多 20 行校验分片、最多 10 行发布分片；相同文件哈希恢复同一批次，业务字段相同但来源不同只给疑似重复警告，不自动删除合法需求。
- 新增 `materials`、`import_mapping_templates`、`import_batches`、`import_rows`、`demands`、`demand_materials` 等 P2 表；需求详情可反查文件 SHA-256、文件名、工作表、源行号和原始行，并关联标准物资。未知物资只产生警告，不伪造价格或静默当作已匹配。
- chunk/validate/publish 统一要求 `expectedVersion`，版本守卫、业务写入和幂等记录在同一 D1 batch 中执行；回归测试覆盖 stale version、并发上传、并发校验和并发发布，失败请求不会留下半批源行、重复需求或错误发布状态。
- P1.2→P2 升级测试验证账号、授权范围、活动会话、自定义设置和审计记录均保留；`0002_p2_import_demands_materials.sql` 已纳入迁移 checksum 锁。
- `/demands` 已连接真实 `DemandsView` 并懒加载，不再显示 P2 占位页；需求页提供 Excel/CSV 导入、映射模板、需求池列表/详情和标准物资新增/查询。懒加载后生产主入口 JS 约 458.65 kB，P2 页面约 62.74 kB，构建不再触发 500 kB 主包警告。
- 完整 `npm run check` 全绿：43/43 Node/workerd+D1 测试、26/26 Vue/Vitest 测试，共 69 项 PASS；TypeScript、前端生产构建和 Worker dry-run 同时通过。
- 未验证：真实 5 万行原始业务文件、真实 Cloudflare D1/Workers 配额和目标地区网络仍属于 P7；P2 不据本地合成测试宣称正式上线。下一步进入 P3 储备转换。

## P3：储备转换（先重点验收）

责任范围：项目/分配/估算/分类表、归并及确认接口、四步转换向导、储备列表与详情。

交付：年度/类别/电压/线路建议分组；人工拆并；标准物资汇总与来源明细双视图；部分分配与剩余需求；物资/施工/其他费用；缺价标记；估算版本；需求类别到储备大类映射与共同费用分摊。

验收：100单位拆为60和40；重复提交及两人并发分配不超过100；相同型号不同单位不合并；未知价格与明确零价区分；类别重复标签不增加金额；混合项目分类金额和已知费用合计相等；单据/明细可反查源表行。

P3 完成记录（2026-09-12，主要实现提交 `bd6e7d2`）：

- 新增 `0003_p3_reserve_projects.sql`，建立储备项目、不可变确认版本、需求物资分配、估算费用、储备大类、类别映射和分类金额分摊；迁移锁已纳入 `0003`。P2→P3 升级测试确认已导入需求、标准物资及来源追溯数据完整保留。
- 实现储备候选和年度/类别/电压/线路归并建议；候选池最多每页 100 条并使用不透明游标继续读取，归并建议由 D1 对完整剩余池聚合，不会只统计当前第一页。单条需求物资可按定点数量拆到多个项目。创建/调整分配均使用数据库约束式事务守卫，60/40 拆分、重复提交、并发抢占和 stale version 均不会超分配或留下半笔数据。
- 项目物资汇总按标准物资 + 单位隔离，相同型号不同单位不会合并；详情保留需求、线路、杆段及源文件/工作表/行号反查。
- 估算使用 `quantity_scaled`、`unit_price_scaled` 和整数分，不使用浮点累计；物资单价 `null` 表示未知、显式 0 表示零价。支持施工费/其他费、估价完整度、精确四舍五入到分及 JS 安全整数溢出拒绝。
- 实现储备大类与需求类别版本化映射、费用行分类建议和共同费用拆分；服务端强制每条已知费用完整分摊，分类金额合计必须等于项目已知估算金额。
- 储备确认生成不可变 `project_versions` 快照并保存确认/修订原因；后续调整回到草稿，再确认形成新的 `reserve_version`，历史版本不被覆盖。
- `/reserves` 已懒加载真实 `ReservesView`，形成“选择需求与归并建议 → 建立草稿 → 核对物资与估算/分类 → 确认储备版本”的实际工作流；只读角色可查看但不能修改。
- 完整 `npm run check` 全绿：58/58 Node/workerd+D1 + 32/32 Vue/Vitest，共 90 项 PASS；TypeScript、Vite 生产构建和 Worker dry-run 同时通过。主入口 JS 约 458.80 kB，`ReservesView` 独立约 18.64 kB。
- 未验证：真实业务需求/价格/分类文件、真实 Cloudflare D1/Workers 配额和目标地区网络仍留 P7；P3 不据合成数据宣称正式上线。下一步进入 P4 框架、协议、预算与费用流水。

## P4：框架、协议及资金

责任范围：框架/协议/预算/流水迁移与API、费用页面、口径切换、金额与日期共享逻辑。

交付：框架年度目标；协议版本与有效期；子项目预算草稿/确认及多协议分配；独立预算发生/实际费用流水；更正冲销；原子记账与幂等；90%、80%和预算超框架事件。

验收：无协议不能确认发生；跨框架拒绝；一协议多子项目、一子项目多协议的金额不重复；预算不自动产生发生额；90%/80%精确边界；预算合计=框架不超额，超1分预警；预警不擅自硬阻断；0分母显示未配置；并发更新不漏记；旧版本不重复计入。

P4 完成记录（2026-09-12，主要实现提交 `17ffcea`）：

- 新增 `0004_p4_finance.sql`，建立框架/框架版本、执行协议/协议版本、项目预算当前态、预算确认版本与协议分配快照、预算发生/实际发生流水及协议金额分摊；迁移 checksum 已锁定。P3→P4 升级测试确认储备项目、需求分配及既有确认版本完整保留。
- 框架与协议使用版本化更新和数据库约束式事务 guard；并发使用同一 `expectedVersion` 时仅一方成功。项目绑定框架后，若已存在确认预算或资金流水，禁止直接改绑到其他框架，避免历史账目失去归属。
- 预算草稿与预算发生严格分离：草稿可不完整；确认预算时必须归属框架，协议分配合计必须精确等于预算总额且协议在确认日有效。确认预算只生成不可变 `budget_versions`/分配快照，不自动生成 `budget_occurrence`。
- 预算确认后的再次调整回到草稿，重新确认产生新的 `budget_version`；资金汇总只计当前生效预算版本，旧版本保留历史但不重复计入。
- `budget_occurrence` 与 `actual_cost` 采用追加式资金流水，必须按金额完整分配到同框架且业务日期有效的协议；同一协议可服务多个子项目，一个项目也可拆到多个协议，服务端按分摊金额汇总，避免重复放大。流水列表最多每页 100 条并使用不透明游标继续读取；单页协议分摊批量查询，框架资金汇总按协议使用固定数量 GROUP BY 查询，避免随协议/流水数量产生 N+1。
- 更正使用独立负数冲销记录并保留原流水；同一原流水最多冲销一次。幂等重放优先于对象版本/已冲销状态判断，同一 Idempotency-Key 重试返回原响应，不重复写版本或流水。
- 阈值判定使用整数交叉相乘而不是显示值四舍五入：协议使用率精确达到 90% 才预警，框架预算发生精确达到 80% 才预警；79.9999% 不提前触发。当前有效预算合计只有大于框架总额才提示超框架，等于不提示、超 1 分提示。0 分母返回“未配置”。阈值均为警示，不阻断合法记账。
- `/finance` 已从占位页切换为懒加载真实 `FinanceView`；页面独立展示预算确认占用、预算发生、实际发生，支持框架/协议管理、项目归属、预算多协议分配/确认和资金流水登记。财务角色可维护预算/流水，但不显示框架结构新增控件。
- 健康接口阶段更新为 `p4`。完整 `npm run check` 全绿：70/70 Node/workerd+D1 + 38/38 Vue/Vitest，共 108 项 PASS；TypeScript、Vite 生产构建和 Worker dry-run 同时通过。主入口 JS 约 458.90 kB，`FinanceView` 独立约 20.09 kB。
- 未验证：真实框架/执行协议/预算资料、真实 Cloudflare D1/Workers 配额及目标地区网络仍留 P7；P4 不据合成数据宣称正式上线。下一步进入 P5 出库、实施、结算与四状态反馈。

## P5：出库、实施、结算与反馈

责任范围：出库/实施/结算及范围关联、状态投影、相应界面和附件元数据/私有存取。

交付：项目出库快照和分批范围、实施完成量与实际物资使用、历史记录补录和待关联、结算覆盖及最终标记、四状态和部分进度、结算待办。储备与子项目沿用同一ID。

验收：四种组合均可表达；分批完成60/100不等于整条已实施；剩余未安排需求仍影响总状态；先结算后实施可用；撤销结算后重新待办；历史实施不造出库且关联后不重复统计；附件跨项目拒绝；已有出库不能被修改储备静默覆盖。

P5 完成记录（2026-09-12，主要实现提交 `8093a00`）：

- 新增 `0005_p5_delivery_implementation_settlement.sql`，建立出库批次/出库行、实施记录/实施行、结算/结算覆盖/结算协议分摊及私有附件元数据；`0005` 已加入迁移 checksum 锁。P4→P5 升级测试确认既有框架、预算版本和资金流水历史不丢失。
- 出库以项目当前需求分配为范围上限，支持分批出库并保存不可变范围快照；项目版本与出库写入同一 D1 batch，两个并发出库请求使用同一 `expectedProjectVersion` 时只能一方成功，不会超出项目分配数量。
- 正常实施必须关联已出库行，完成量不能超过对应已出库数量；历史实施允许先不关联项目/出库，后续显式关联项目和需求物资，不补造出库，也不会因为重复关联产生二次统计。
- 实施与结算完全独立：允许先结算后实施。四状态按实施完成和结算完成两个维度投影为“已实施已结算 / 已实施未结算 / 未实施已结算 / 未实施未结算”；部分完成仅显示进度，不会把 60/100 错判成完成。
- “已结算”必须同时满足全部有效范围覆盖完整且存在有效最终结算。非最终结算即使覆盖 100% 仍保持未结算；若既有非最终结算已完整覆盖，可用 0 元、空新增覆盖的最终确认正式收口。最终结算撤销后重新投影为未结算，并恢复结算待办。
- 实施全部完成后生成默认 30 天结算待办；只有真正达到最终结算完成才关闭。最终结算标记本身不能伪造覆盖完整，创建最终结算时服务端会校验完整范围。
- P3 修改需求分配时会读取 P5 已出库、已实施和有效结算的保护下限；不能把当前项目范围缩小到既有历史事实以下。P3/P5 写入统一推进同一个 `projects.version`，避免并发修改绕过保护。
- `/delivery` 已从占位页切换为真实懒加载 `DeliveryView`，支持项目生命周期总览、分批出库、正常/历史实施、历史关联、结算/撤销和项目私有附件。项目管理、实施、财务和只读角色按服务端边界分别开放操作。
- 附件存私有 R2，单文件最大 10 MiB；元数据写入失败会删除对应 R2 对象，下载必须重新通过项目范围授权，跨项目访问返回 403。
- 健康接口阶段更新为 `p5`。完整 `npm run check` 全绿：80/80 Node/workerd+D1 + 43/43 Vue/Vitest，共 123 项 PASS；TypeScript、Vite 生产构建和 Worker dry-run 同时通过。主入口 JS 约 459.00 kB，`DeliveryView` 独立约 19.83 kB。
- 未验证：真实出库/实施/结算历史、真实附件规模、真实 Cloudflare D1/R2 配额和目标地区网络仍留 P7；P5 不据合成数据宣称正式上线。下一步进入 P6 分析、提醒与备份。

## P6：分析、提醒及备份

责任范围：报表/计划/事项/预警/outbox/备份任务、六模块首页与分析页面、ECharts图表、通知投递适配器。

交付：月计划和季度累计目标；可配置的统计口径/滞后规则；月报版本快照；子项目缺口排名；现状储备剩余范围分类；年度事项的月/日精度；后台每5分钟小批处理；首次告警、每日摘要、恢复与再次越线；已验证通知地址；D1导出R2备份、manifest 与附件清单。

验收：跨年、月末、季度最后一天和次日；过去季度不得显示未到；按同期计划80%与落后百分点两种规则正确；0年度目标待配置；只知道月份不捏造日期；规则改动不覆写历史报表；重复触发、领取超时、失败退避、发送结果未知均可恢复；关闭浏览器仍有后台任务；备份故障可见且一次实际恢复成功。

P6 完成记录（2026-09-12，主要实现提交 `3f4a62d`）：

- 新增 `0006_p6_analysis_notifications_backups.sql`，建立分析规则、月计划、月报快照、年度事项、通知联系人、预警事件、通知 outbox、备份任务和备份分片；迁移 checksum 已锁定，并通过 P5→P6 数据保留与空库重复迁移测试。
- 实现分析首页/分析页真实数据、月计划、两种滞后规则、季度状态、子项目缺口、储备剩余按未出库比例折算与分类金额守恒；历史月报保存规则版本和完整快照，规则调整不会覆盖旧修订。
- 年度事项支持 `month/day/unknown` 日期精度，不捏造日期；预警生命周期覆盖首次 crossing、持续期间每日摘要、recovery、同周期 recross；通知 outbox 支持租约、超时回收、指数退避和 `unknown` 结果留痕。
- Worker `scheduled()` 每 5 分钟运行服务端 tick；未配置 `NOTIFICATION_DELIVERY_URL` 时不领取待发消息，配置后按小批投递，2xx 记 sent、明确非 2xx 记 failed、网络结果不确定记 unknown。真实邮件供应商和域名仍留 P7。
- D1→R2 逻辑备份按表和 100 行分片导出，保存 chunk SHA-256 与 manifest，并记录附件对象 key；自动 daily/monthly 备份由 Asia/Shanghai 03:00 对应 tick 创建。测试已从真实 manifest/chunk 恢复到第二个独立临时 D1 并对账项目、资金、出库、实施、结算和规则，同时明确不恢复 `auth_sessions`。
- `/analysis` 已接真实懒加载页面，Dashboard 使用真实统计。ECharts 改为按需注册并二次懒加载：`AnalysisView` 约 19.48 kB（gzip 6.25 kB），独立 `echarts` 运行块约 488.44 kB（gzip 164.84 kB），生产构建无 >500 kB chunk 告警；Worker dry-run 总上传约 411.87 KiB（gzip 78.17 KiB）。
- 最终分段完整门禁全绿：93/93 Node/workerd+D1 + 48/48 Vue/Vitest，共 141 项 PASS；TypeScript、Vite 生产构建和 Worker `wrangler deploy --dry-run` 同时通过。因单次 `npm run check` 会受当前工具 300 秒上限影响，Node 测试按文件组完整覆盖，未跳过任何 `tests/*.test.mjs`。
- 未验证：真实业务 Excel/年度节点/分类数据、真实邮件供应商/域名、真实 Cloudflare Workers/D1/R2 CPU 与免费额度、目标地区网络、正式环境恢复与运维移交，全部进入 P7；不得用 P6 本地合成结果宣称正式上线。

## P7：真实数据、免费额度与上线

责任范围：标准需求模板下载/回导闭环与真实业务值抽检、集成/端到端/性能、生产配置、网络与费用验证、运行手册，以及 Cloudflare/GitHub 正式运维移交与 CI/CD 服务身份。

交付前必须有按系统标准模板填报的代表性真实需求数据，以及年度节点/类别/框架协议等需要人工核对的真实业务资料；同时需要可用 Cloudflare 账户资源/域名、受托运维身份及通知收件配置。真实业务文件仅在受控本地/私有存储中验收，不提交 GitHub。缺少任一上线必需项时记录具体未完成项，不宣称正式上线。

运维移交要求：交接时由现有 Super Administrator 完成一次性基础授权，至少指定一名受托技术运维负责人使用其自己的 Cloudflare 成员身份；若要求其今后能独立管理 Cloudflare 成员和账户级 CI Token，则需明确授予足够权限并记录风险。建立 Cloudflare account-owned API token 作为 CI/CD 服务身份，按最小权限写入 GitHub Secrets/Environment Secrets；生产发布不得依赖资产所有者个人登录态、Global API Key 或长期个人 Token。完成后实际演练“资产所有者不登录 Cloudflare”的发布、迁移、回退、基础设施权限调整和旧维护人撤权。

验收矩阵：

| 领域 | 标准 |
|---|---|
| 标准模板回导 | 下载系统模板、按模板填报并回导；抽样逐项核对数量、类别、来源行和需求字段，模板字段无需依赖历史 Excel 猜测映射 |
| 资金 | 协议/子项目/框架汇总能与流水逐笔对账 |
| 权限 | 身份、角色、项目范围、附件访问无绕过路径；首管理员、最后管理员保护和成员停用均有效 |
| 数据规模 | 5万行需求、5人并发，无全量加载或重复写入 |
| Workers Free | 真实部署测CPU分布及错误率，不用本地耗时冒充10msCPU验证 |
| D1/R2 | 测读写/索引/审计/备份占用，验证70%告警和容量计划 |
| 大陆网络 | 实际用户所在地电信/联通/移动测试登录、列表、文件及邮件 |
| 用户体验目标 | 常用列表P95≤3秒，已登录首页主要内容≤5秒；未达标先记录与优化，不虚报 |
| 恢复 | D1恢复演练，并验证R2附件清单与实际文件可对应 |
| 发布 | 明确版本、迁移顺序、回退办法和监控责任人；受托维护人/CI 可在不使用资产所有者 Cloudflare 登录态的情况下完成一次正式受控发布 |
| 运维移交 | Cloudflare 成员权限、GitHub 权限、account-owned CI Token、Secret 位置、Token 轮换、旧维护人撤权和紧急恢复均有书面清单并实测一次 |

## 阶段完成记录格式

每阶段结束更新本文件状态和 `AI_HANDOFF.md`，包括提交号、完成能力、实际运行的检查、未验证项、下一阶段入口。不能只写“完成”；必须能从代码、测试和运行结果核对。

### P7 离线准备 · 2026-09-12

- 完成：非 Secret production config 模板与拒绝占位/错误绑定/旧认证的校验；13 项真实验收记录模板/结构检查；私有文件哈希清单；P6 manifest/chunk/附件存在性离线检查；发布、迁移、停写备份、隔离恢复、回退和运维撤权手册。
- CI：普通 push 仍只验证；新增手工 production preflight 仅 dry-run，不使用云 Token。生产部署模板位于 workflows 外，必须另行授权、配置并验证 Environment 保护后才可启用。
- 缺陷：真实 Excel 准备审计发现解析器忽略空行和 used-range 起点会偏移物理来源行。两个回归先复现失败，再修复保留真实行号且过滤空行；不自动改写旧批次。
- 验证：当前门禁 100/100 Node/workerd+D1 + 58/58 Vue/Vitest，共 158/158 PASS；TypeScript、Vite build、Worker dry-run PASS。整条 `npm run check` 因工具单次 300 秒上限被截断，随后以类型+构建+全部前端测试、Node 36+64 两组等价完整执行且无跳过。新增统一 API 响应解析：开发代理/API 临时返回非 JSON 或畸形 JSON 时显示稳定中文错误，不再向用户暴露 `Unexpected token`。此前 production 示例独立 dry-run、3 份 workflow YAML 语法解析、空验收记录退出 2、占位 production config 退出 1 的 P7 预检结果继续有效。
- 迁移影响：无 schema/迁移改动；认证与 P2–P6 后端数量、金额、版本、幂等、并发和状态实现保持不变。
- 未完成：P7-01 的最终真实业务值抽检，以及 P7-02～13 的真实业务资料、生产资源/授权、Secret、邮件适配器/域名、CPU/额度与现场网络、真实恢复/发布/撤权证据仍待完成。需求字段结构由系统标准模板定义，不再等待任意历史 Excel。没有创建资源、部署或发送邮件，未使用生产 Secret。
- 下一步：按 `docs/P7_RUNBOOK.md` 和矩阵采集真实证据；JSON 记录结构通过不能代替真实验收。备份不具备跨表快照隔离，正式迁移前需可靠停写并隔离恢复对账。
